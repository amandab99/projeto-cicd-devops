name: Continuous Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Deploy Simulado
      run: |
        echo "Iniciando deploy fictício..."
        echo "Deploy realizado com sucesso!"
